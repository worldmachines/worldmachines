.DS_Store
.env
.env.keys
website/.dev.vars
.wrangler/
.opentrace/
.opentrace-index.log

# Node deps (wrangler installed via npm at repo root). package.json /
# package-lock.json get committed; node_modules does not.
node_modules/

# Local agent research and test exports (do not commit).
.agent-research/
website/notes-data/
notes.parquet

# uv-managed tool venvs.
tools/**/.venv/
