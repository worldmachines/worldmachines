# Local preview only. Copy to website/.dev.vars (gitignored, never commit real
# values) and fill in the two values below.
#
# Real dev values live in wm-encyclopedia-kb/.env — ASK_TOKEN corresponds to
# ASK_TOKEN_DEV there. ORACLE_URL is the deployed wm-oracle-dev worker
# documented in wm-encyclopedia-kb/docs/TESTING-DEV-STACK.md.

ORACLE_URL=https://wm-oracle-dev.example.workers.dev
ASK_TOKEN=replace-with-ASK_TOKEN_DEV
